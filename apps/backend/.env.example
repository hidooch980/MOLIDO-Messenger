# Copy to .env for local development. Never commit the real .env.
DATABASE_URL="postgresql://molido:molido_dev_password@localhost:5432/molido_dev"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="change-me-in-every-environment"
PORT=4000

# Revenue features must stay disabled by default in every environment
# until a monetization phase explicitly turns them on (spec section 37).
REVENUE_ENABLED=false
